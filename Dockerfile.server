FROM python:3.11-slim

WORKDIR /app

RUN apt-get update && apt-get install -y git && rm -rf /var/lib/apt/lists/*

COPY . /app/
RUN pip install --no-cache-dir . fastapi uvicorn websockets

COPY . /app/

EXPOSE 8000

CMD ["uvicorn", "aegis_server.main:app", "--host", "0.0.0.0", "--port", "8000"]
